{
  "name": "tool-registry-scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Generator for the ERC-8257 Tool Registry reference test vectors. Self-contained: install and run in place without affecting the parent project.",
  "type": "module",
  "scripts": {
    "generate": "tsx generate-test-vectors.ts"
  },
  "dependencies": {
    "@noble/hashes": "2.0.1",
    "canonicalize": "2.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^6.0.2"
  }
}
